import { DmoDirective } from './dmo.directive';

describe('DmoDirective', () => {
  it('should create an instance', () => {
    const directive = new DmoDirective();
    expect(directive).toBeTruthy();
  });
});
